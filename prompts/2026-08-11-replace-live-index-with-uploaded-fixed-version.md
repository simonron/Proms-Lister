# User prompt

Go on then

# Context

The user re-uploaded the corrected `index.html` after GitHub Pages deployment was repaired. The requested action was to use that uploaded corrected file as the live repository `index.html`, rather than continuing to layer patches over the older broken Home menu implementation.

# Change

Replaced `index.html` on `main` with the uploaded corrected version. This version has no Home hamburger menu and instead exposes Import & Transfer and Calendar directly on the Home page.