# User prompt

Do it

# Context

The user asked to add a proper GitHub Pages deployment workflow so that the root of `main` is deployed on every push, making deployment failures visible in GitHub Actions.

# Change requested

Add an automatic GitHub Pages workflow deploying the repository root from `main` on every push.