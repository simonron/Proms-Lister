/* Royal Albert Hall seat map — 1346x1536 map geometry.
   SCRIPT ONLY. */
(function(){
'use strict';
const MAP_REV='RAH-1346x1536-20260808D';
const EXPECT_W=1346, EXPECT_H=1536;
const IMAGE_URL='/Proms-Lister/rah-seating-plan.jpg?map='+encodeURIComponent(MAP_REV);
const STORE='rahUserSeatCorrections:'+MAP_REV;
const LOOKUP={};
(function(){const row=7,anchorSeat=141,anchorX=72.3221588764,anchorY=31.2775528198,stepX=-0.4065618346,stepY=-0.97884648;for(let seat=84;seat<=153;seat++){const d=seat-anchorSeat;LOOKUP['O|'+row+'|'+seat]={x:anchorX+d*stepX,y:anchorY+d*stepY,section:'O',row,seat,kind:seat===anchorSeat?'new-image anchor':'same-row indexed',mapRev:MAP_REV}}})();
function clean(v){return String(v||'').toUpperCase().replace(/STALLS|CIRCLE|SECTION|ARENA/g,'').replace(/[^A-Z]/g,'').trim()}
function num(v){const m=String(v==null?'':v).match(/\d+/);return m?+m[0]:NaN}
function key(s,r,n){return s+'|'+r+'|'+n}
function loadCorrections(){try{return JSON.parse(localStorage.getItem(STORE)||'{}')}catch(e){return{}}}
function saveCorrections(c){localStorage.setItem(STORE,JSON.stringify(c))}
function allPoints(){return Object.values(Object.assign({},LOOKUP,loadCorrections()))}
function locate(t){const s=clean(t&&(t.section||t.area)),r=num(t&&t.row),n=num(t&&t.seat),k=key(s,r,n);return loadCorrections()[k]||LOOKUP[k]||null}
function nearest(x,y){let best=null,bd=Infinity;for(const p of allPoints()){const d=(p.x-x)*(p.x-x)+(p.y-y)*(p.y-y);if(d<bd){bd=d;best=p}}return best?Object.assign({},best,{distance:Math.sqrt(bd)}):null}
function pct(ev,img){const r=img.getBoundingClientRect();return{x:(ev.clientX-r.left)*100/r.width,y:(ev.clientY-r.top)*100/r.height}}
function parseSeat(s){const u=String(s||'').toUpperCase(),dm=u.match(/DOOR\s*[:#-]?\s*([A-Z0-9]+)/),rm=u.match(/ROW\s*[:#-]?\s*(\d+)/),sm=u.match(/SEAT\w*\s*[:#-]?\s*(\d+)/);let sec='';const xm=u.match(/(?:STALLS|SECTION|CIRCLE)\s*([A-Z]{1,2})\b/);if(xm)sec=xm[1];if(!sec){const m=u.match(/\b([A-Y])\b/);if(m)sec=m[1]}return sec&&rm&&sm?{door:dm?dm[1]:'',section:sec,row:+rm[1],seat:+sm[1]}:null}
function currentTicket(){try{if(typeof selected!=='undefined'&&selected&&typeof ticketFor==='function')return ticketFor(selected)||{}}catch(e){}const l=document.getElementById('seatLabel');return parseSeat(l&&l.textContent)||{}}
function ensureMarker(host){let m=document.getElementById('rahAutoSeatMark');if(!m){m=document.createElement('div');m.id='rahAutoSeatMark';m.style.cssText='position:absolute;width:18px;height:18px;border-radius:50%;background:#e00000;border:3px solid white;box-shadow:0 0 0 2px #e00000,0 2px 7px #000;transform:translate(-50%,-50%);z-index:35;pointer-events:none;display:none';host.appendChild(m)}return m}
function imageIsCurrent(img){return img.naturalWidth===EXPECT_W&&img.naturalHeight===EXPECT_H}
function renderMarker(){const img=document.getElementById('rahImg'),host=document.getElementById('pi')||(img&&img.parentElement);if(!img||!host)return;const m=ensureMarker(host),legacy=document.getElementById('seatMark'),label=document.getElementById('seatLabel'),t=currentTicket();if(legacy)legacy.style.display='none';if(!imageIsCurrent(img)){m.style.display='none';return}const p=locate(t);if(p){m.style.display='block';m.style.left=p.x+'%';m.style.top=p.y+'%'}else m.style.display='none';if(label){const d=[t.door&&'Door '+t.door,t.section||t.area,t.row&&'Row '+t.row,t.seat&&'Seat '+t.seat].filter(Boolean).join(' · ');label.textContent=p?d+' — '+p.kind:d+' — position not yet indexed'}}
let pending=null;
function startCorrection(){const t=currentTicket(),def=[t.door&&'Door '+t.door,t.section||t.area,t.row&&'Row '+t.row,t.seat&&'Seat '+t.seat].filter(Boolean).join(' '),s=prompt('Which seat are you correcting?\nExample: Door 9 Stalls O Row 7 Seat 141',def);if(s===null)return;pending=parseSeat(s);if(!pending){alert('Please enter Section, Row and Seat, e.g. Stalls O Row 7 Seat 141.');return}const st=document.getElementById('rahCorrectionStatus');if(st)st.textContent='Click the exact centre of Stalls '+pending.section+' · Row '+pending.row+' · Seat '+pending.seat}
function undoCorrection(){const t=currentTicket(),k=key(clean(t.section||t.area),num(t.row),num(t.seat)),c=loadCorrections();if(c[k]){delete c[k];saveCorrections(c);renderMarker()}else alert('This seat has no user correction on this map.')}
function exportCorrections(){const payload={mapRevision:MAP_REV,image:{width:EXPECT_W,height:EXPECT_H},corrections:loadCorrections()},blob=new Blob([JSON.stringify(payload,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='RAH-seat-corrections-'+MAP_REV+'.json';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}
function importCorrections(){const i=document.createElement('input');i.type='file';i.accept='.json,application/json';i.onchange=()=>{const f=i.files&&i.files[0];if(!f)return;const r=new FileReader();r.onload=()=>{try{const x=JSON.parse(r.result),rev=x.mapRevision||'',data=x.corrections||x;if(rev&&rev!==MAP_REV)return alert('Corrections belong to '+rev+', not '+MAP_REV+'.');saveCorrections(Object.assign(loadCorrections(),data));renderMarker()}catch(e){alert('Invalid corrections file.')}};r.readAsText(f)};i.click()}
function addControls(host){if(document.getElementById('rahCorrectionBar'))return;const bar=document.createElement('div');bar.id='rahCorrectionBar';bar.style.cssText='position:absolute;right:10px;top:10px;z-index:40;display:flex;gap:6px;flex-wrap:wrap;justify-content:flex-end';[['Correct seat',startCorrection],['Undo',undoCorrection],['Export',exportCorrections],['Import',importCorrections]].forEach(([txt,fn])=>{const b=document.createElement('button');b.type='button';b.textContent=txt;b.style.cssText='padding:6px 9px;border-radius:6px;border:1px solid #777;background:#fff;color:#111';b.onclick=e=>{e.preventDefault();e.stopPropagation();fn()};bar.appendChild(b)});const st=document.createElement('span');st.id='rahCorrectionStatus';st.style.cssText='width:100%;text-align:right;background:rgba(255,255,255,.92);padding:4px 7px;border-radius:5px;font:12px -apple-system,BlinkMacSystemFont,Segoe UI,sans-serif';st.textContent='Seat corrections saved in this browser';bar.appendChild(st);host.appendChild(bar)}
function install(){const img=document.getElementById('rahImg'),host=document.getElementById('pi')||(img&&img.parentElement);if(!img||!host)return;addControls(host);img.style.filter='none';img.style.cursor='crosshair';if(!String(img.src).includes('map='+encodeURIComponent(MAP_REV)))img.src=IMAGE_URL;let read=document.getElementById('rahHoverReadout');if(!read){read=document.createElement('div');read.id='rahHoverReadout';read.style.cssText='position:fixed;display:none;z-index:100002;pointer-events:none;background:rgba(0,0,0,.9);color:#fff;padding:7px 10px;border-radius:6px;font:600 13px -apple-system,BlinkMacSystemFont,Segoe UI,sans-serif;white-space:nowrap';document.body.appendChild(read)}if(img.dataset.rahMapRev!==MAP_REV){img.dataset.rahMapRev=MAP_REV;const move=ev=>{if(!imageIsCurrent(img)){read.style.display='none';return}if(pending){read.textContent='Click to place Stalls '+pending.section+' · Row '+pending.row+' · Seat '+pending.seat;read.style.display='block'}else{const q=pct(ev,img),p=nearest(q.x,q.y);if(p&&p.distance<1.1){read.textContent='Stalls '+p.section+' · Row '+p.row+' · Seat '+p.seat+' · '+p.kind;read.style.display='block'}else read.style.display='none'}if(read.style.display==='block'){read.style.left=Math.max(4,Math.min(innerWidth-320,ev.clientX+14))+'px';read.style.top=Math.max(4,Math.min(innerHeight-42,ev.clientY+14))+'px'}};img.addEventListener('mousemove',move);img.addEventListener('pointermove',move);img.addEventListener('mouseleave',()=>read.style.display='none');img.addEventListener('click',ev=>{if(!pending||!imageIsCurrent(img))return;ev.preventDefault();ev.stopPropagation();const q=pct(ev,img),c=loadCorrections(),k=key(pending.section,pending.row,pending.seat);c[k]={x:q.x,y:q.y,door:pending.door,section:pending.section,row:pending.row,seat:pending.seat,kind:'user corrected',mapRev:MAP_REV};saveCorrections(c);pending=null;renderMarker()});img.addEventListener('load',renderMarker)}if(img.complete)renderMarker()}
document.addEventListener('click',ev=>{const el=ev.target&&ev.target.closest&&ev.target.closest('button,a');if(el&&/^RAH (interactive )?seating plan/i.test((el.textContent||'').trim()))setTimeout(install,0)},true);
window.RAHSeatMap={mapRevision:MAP_REV,install,locate,nearest,startCorrection,renderMarker,lookup:LOOKUP};
})();